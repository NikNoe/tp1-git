#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("Welcome in cours de git");
   //ajout d'un commentaire   
   //ajout d'une ligne sans faire de commit
   //repertoire de travail et commit index
   return 0;
}
